// Адрес wasm-файла решателя ограничений.
//
// Сам решатель planegcs собран в wasm (двоичный модуль, который браузер
// исполняет напрямую). По умолчанию библиотека ищет этот файл рядом с собой,
// и в браузере такой путь не находится — вместо wasm приезжает страница
// приложения. Поэтому адрес получаем у сборщика: `?url` заставляет Vite
// положить файл в сборку и вернуть готовую ссылку.
//
// Импорт живёт здесь, а не в адаптере, чтобы адаптер оставался обычным
// модулем и его можно было проверять тестами в Node без сборщика.
import wasmUrl from '@salusoft89/planegcs/dist/planegcs_dist/planegcs.wasm?url';

export const PLANEGCS_WASM_URL: string = wasmUrl;
